const C=document.getElementById('space'),S=new THREE.Scene(),cam=new THREE.PerspectiveCamera(60,innerWidth/innerHeight,.1,2500),R=new THREE.WebGLRenderer({canvas:C,antialias:true,alpha:true});R.setPixelRatio(Math.min(devicePixelRatio,2));R.setSize(innerWidth,innerHeight);cam.position.z=12;const g=new THREE.BufferGeometry(),N=9000,p=new Float32Array(N*3);for(let i=0;i<N;i++){p[i*3]=(Math.random()-.5)*180;p[i*3+1]=(Math.random()-.5)*110;p[i*3+2]=-Math.random()*220}g.setAttribute('position',new THREE.BufferAttribute(p,3));S.add(new THREE.Points(g,new THREE.PointsMaterial({color:0xffffff,size:.055,opacity:.8,transparent:true})));const planet=new THREE.Mesh(new THREE.SphereGeometry(2.3,64,64),new THREE.MeshStandardMaterial({color:0x24104d,roughness:.65,metalness:.2,emissive:0x160a33,emissiveIntensity:1.2}));planet.position.set(5,-1,-10);S.add(planet);const ring=new THREE.Mesh(new THREE.TorusGeometry(3.2,.025,12,160),new THREE.MeshBasicMaterial({color:0x8b5cf6,transparent:true,opacity:.7}));ring.rotation.x=.8;ring.position.copy(planet.position);S.add(ring);const light=new THREE.PointLight(0x8b5cf6,80,50);light.position.set(3,5,3);S.add(light);S.add(new THREE.AmbientLight(0x6677aa,1.2));let tx=0,ty=0,sy=0;addEventListener('pointermove',e=>{tx=(e.clientX/innerWidth-.5)*2;ty=(e.clientY/innerHeight-.5)*2});addEventListener('scroll',()=>sy=scrollY);document.querySelectorAll('[data-c]').forEach(b=>b.onclick=()=>{let c=b.dataset.c;document.documentElement.style.setProperty('--a',c);light.color.set(c);ring.material.color.set(c);localStorage.a=c});document.getElementById('mode').onclick=()=>{document.body.classList.toggle('day');localStorage.m=document.body.classList.contains('day')?'day':'night'};if(localStorage.a){document.documentElement.style.setProperty('--a',localStorage.a);light.color.set(localStorage.a);ring.material.color.set(localStorage.a)}if(localStorage.m==='day')document.body.classList.add('day');function loop(){requestAnimationFrame(loop);g.rotation.y+=.00035;planet.rotation.y+=.002;ring.rotation.z-=.001;cam.position.x+=(tx*1.1-cam.position.x)*.025;cam.position.y+=(-ty*.7-cam.position.y)*.025;cam.position.z+=(12-Math.min(sy*.0035,7)-cam.position.z)*.035;ring.position.copy(planet.position);R.render(S,cam)}loop();addEventListener('resize',()=>{cam.aspect=innerWidth/innerHeight;cam.updateProjectionMatrix();R.setSize(innerWidth,innerHeight)})